# SSH connection details
[ssh]
host=205.196.19.26
port=11652
user=shawnghu
identity=~/.ssh/id_ed25519

# Base directories
[settings]
local_base=/home/shawnghu
remote_base=/home/shawnghu

# Interval between rsync runs (seconds)
interval=60

# Directories to sync (relative to base dirs above)
# Format: path [exclude_pattern1,exclude_pattern2,...]
[dirs]
small-rl/output checkpoint*
small-rl/benchmarks
.claude/projects
small-rl/wandb
